


local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i---[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%


local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) ==--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i =--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte =--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1)--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t =--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 997--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

----[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "LyoqCiAgICBLY--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "LyoqCiAgICBLYWlIdWIg5byA5aeL55So5oi3IOaPkOS6pOacrOaTjeS9nOeahOaViOeOhwogICAgPT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "LyoqCiAgICBLYWlIdWIg5byA5aeL55So5oi3IOaPkOS6pOacrOaTjeS9nOeahOaViOeOhwogICAgPT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09Ki8KbG9jYWwgUGxheWVycyA9IGdhbWU6R2V0U2Vydmlj--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "LyoqCiAgICBLYWlIdWIg5byA5aeL55So5oi3IOaPkOS6pOacrOaTjeS9nOeahOaViOeOhwogICAgPT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09PT09Ki8KbG9jYWwgUGxheWVycyA9IGdhbWU6R2V0U2VydmljZSgiUGxheWVycyIpO--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "--[[
    KaiHub 加密远程加载器
    使用方法: loadstring(game:HttpGet("你的链接/Ui_Encrypted.lua"))()
]]

local function decode(str)
    local b64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
    str = string.gsub(str, '[^'..b64..'=]', '')
    return (str:gsub('.', function(x)
        if x == '=' then return '' end
        local r, f = '', b64:find(x) - 1
        for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
        return r
    end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
        if #x ~= 8 then return '' end
        local c = 0
        for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
        return string.char(c)
    end))
end

local function xorDecrypt(str, key)
    local result = {}
    for i = 1, #str do
        local byte = str:byte(i)
        local keyByte = key:byte(((i - 1) % #key) + 1)
        table.insert(result, string.char(bit32.bxor(byte, keyByte)))
    end
    return table.concat(result)
end

local function getKey()
    local t = tick()
    local s = tostring(math.floor(t * 1000) % 9973)
    return s:sub(1, 4)
end

-- 加密后的代码 (base64)
local encrypted = "